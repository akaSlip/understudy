/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

// Injected at build time (see vite.config.ts) so the app can display its build.
declare const __BUILD_STAMP__: string
